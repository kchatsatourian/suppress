{
    "build": {
        "dockerfile": "Dockerfile"
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "EditorConfig.EditorConfig",
                "golang.go"
            ],
            "settings": {
                "editor.formatOnSave": true
            }
        }
    },
    "name": "Suppress"
}
